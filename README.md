# fastapi-demo
fastapi => The API web framwork
unicorn => the ASGI web server used to run FastApi
pydantic => validate request and response data ex> task title is a string etc
pydantic-settings => Loads application settings from environment variables and .env files
python-json-logger => produces strcuture json application logs then the plain-yexy logs.
